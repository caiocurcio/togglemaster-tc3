# Sem anotacao de IRSA: o "flag" nao fala com nenhum servico da AWS em
# runtime (so com o Postgres, que autentica por usuario/senha, nao IAM).
apiVersion: v1
kind: ServiceAccount
metadata:
  name: flag
  namespace: togglemaster
