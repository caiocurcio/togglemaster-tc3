apiVersion: v1
kind: Service
metadata:
  name: analytics
  namespace: togglemaster
  labels:
    app: analytics
spec:
  type: ClusterIP
  selector:
    app: analytics
  ports:
    - port: 5000
      targetPort: 5000
