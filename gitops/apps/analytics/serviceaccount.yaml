# Mesmo cuidado do evaluation: o nome "analytics" tem que bater com a
# trust policy em infra/envs/dev/main.tf
# ("system:serviceaccount:togglemaster:analytics").
apiVersion: v1
kind: ServiceAccount
metadata:
  name: analytics
  namespace: togglemaster
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::383996694340:role/togglemaster-analytics-irsa
